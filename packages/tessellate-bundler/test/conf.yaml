appPort: 3001
logLevel: debug
publishTarget: file://bundles
# Node modules to load in webpack-sandboxed. Must be locally installed.
npmModules:
  - mosaic-component-example
# Node modules to exclude as webpack externals from the compiled bundles.
# npmExternals:
#   - mosaic-component-example
